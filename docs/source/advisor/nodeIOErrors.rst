IO Errors can indicate a node with a faulty disk
-------------------------------------------------
IO Errors are errors that originate from the hardware, most likely from a faulty disk, check that specific node hardware.
